{
  "name": "gju-reserve",
  "version": "0.1.0",
  "private": true,
  "description": "GJU-reserve mobile-first reservation product prototype",
  "scripts": {
    "dev": "node server.mjs",
    "start": "node server.mjs",
    "build": "node scripts/build-static.js",
    "seed:demo": "node scripts/seed-demo-data.js",
    "check": "node --check core.mjs && node --check server.mjs && node --check worker.mjs",
    "test:security": "node scripts/security-smoke-test.mjs",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev",
    "upload:dothome": "bash scripts/upload-dothome.sh"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "wrangler": "^4.100.0"
  }
}
