{
  "name": "gju-reserve",
  "version": "0.1.0",
  "private": true,
  "description": "GJU-reserve mobile-first reservation product prototype",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "build": "node scripts/build-static.js",
    "seed:demo": "node scripts/seed-demo-data.js",
    "check": "node --check server.js && node --check worker.mjs",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev",
    "upload:dothome": "bash scripts/upload-dothome.sh"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "wrangler": "^4.100.0"
  }
}
